export const environment = {
  production: true,
  // TODO: point this at the deployed backend's real URL once one exists.
  apiUrl: 'http://localhost:8081/api',
  appName: 'صيدليتي الذكية',
  tokenKey: 'accessToken',
  refreshTokenKey: 'refreshToken',
  userKey: 'currentUser'
};
